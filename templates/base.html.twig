<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>LuksBlog{% block title %}{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <script src="https://cdn.tailwindcss.com"></script>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class=" bg-gray-100">

        
    <header class="m-5 flex justify-between"><div class="ml-5 font-bold text-gray-700 text-4xl"><a href="{{path('app_blog')}}">Luks</a><a href="{{path('app_blog')}}"" class="text-green-400">Blog</a></div>
    <div>

    </div>

    <div>
        <ul class="flex">
            <li class="mx-2 inline-block">
                <a class="p-2 rounded-md inline-block text-white font-bold bg-green-300 hover:bg-green-500 transition delay-100" href="/{{ app.user ? 'logout' : 'login' }}">{{ app.user ? 'Logout' : 'Login' }}</a>
            </li>
            <li>
                {% if app.user is same as(null) %}
                <li class="mx-2 inline-block">
                    <a class="p-2 rounded-md inline-block text-white font-bold bg-green-300 hover:bg-green-500 transition delay-100" href="/register">{{ app.user ? '' : 'Register' }}</a>
            </li>
            {% endif %}
        </ul>
    </div>
</header>
<hr class="b-2 border-gray-300 mx-28">
{% if app.user %}
<div class="text-gray-400 mx-28 flex justify-between">Logged as: {{app.user.email}} <div class=""><a class="mr-10 inline-block"> My posts </a><a class=" inline-block"> All posts </a></div></div>
    <hr class="b-2 border-gray-300 mx-28">
{% endif %}

{% for message in app.flashes('success') %}
        
<div class="m-5 rounded-md p-2 border-green-300 bg-green-50 border border-green-600"> {{ message }}</div>

{% endfor %}
        <div class="flex justify-center">
            <div class="w-2/3 my-5">


        {% block body %}{% endblock %}
            </div>
        </div>
        <hr class="b-2 border-gray-300 mx-28">
        <div class="flex justify-center mt-5 items-center">
        <footer class="text-gray-500">®{{ year }} Łukasz Lechna
        </footer>
        </div>
        <div class="flex justify-center items-center">
            <div class="h-8 w-8 hover:bg-gray-300 p-1 delay-100 transition rounded m-2">
            <a href="https://www.facebook.com/people/%C5%81ukasz-Lechna/100010439683387/">
            <img class="" src="{{asset('build/images/facebook.54d4.png')}}" >
            </a>    
            </div>
            <div class="h-8 w-8 hover:bg-gray-300 delay-100 transition p-1 rounded m-2">
                <a href="https://www.instagram.com/lukaszlechna/">
                <img class="" src="{{asset('build/images/instagram.776d.png')}}" ">
                </a>
            </div>



        </div>
    </body>
</html>
